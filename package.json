{
  "name": "typescript-chatbot-widget",
  "version": "1.0.0",
  "private": true,
  "description": "A simple floating chatbot widget built with TypeScript.",
  "scripts": {
    "build": "npx tsc",
    "start": "npx http-server . -c-1"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
